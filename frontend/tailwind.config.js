

/** @type {import('tailwindcss').Config} */

module.exports = {
  mode: 'jit', // Enable JIT mode
  purge: ['./src/**/*.{js,jsx,ts,tsx}', './public/index.html'],
  theme: {
    extend: {
      colors:{
        
      }
    },
  },
  variants: {},
  plugins: [],
};
